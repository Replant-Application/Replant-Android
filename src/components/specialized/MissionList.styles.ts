/**
 * MissionList 스타일
 * 미션 리스트 컴포넌트의 모든 스타일 정의
 */

import { StyleSheet } from 'react-native';
import { spacing } from '../../utils/designTokens';

export const styles = StyleSheet.create({
  container: {
    gap: spacing[3],
  },
});
