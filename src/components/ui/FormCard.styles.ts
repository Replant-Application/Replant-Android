/**
 * FormCard 스타일
 * 폼 카드 컴포넌트의 모든 스타일 정의
 */

import { StyleSheet } from 'react-native';
import { spacing } from '../../utils/designTokens';

export const styles = StyleSheet.create({
  formCard: {
    marginBottom: spacing[4],
  },
});
