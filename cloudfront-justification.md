# CloudFront 도입 근거: 지연과 비용 비효율

## 1. 지연 시간 문제의 근거

### 코드에서 확인된 증거

#### 타임아웃 설정
```typescript
// src/api/client.ts
const timeoutMs = options.timeout ?? API_CONFIG.timeout; // 기본 10초
const controller = new AbortController();
const timeoutId = setTimeout(() => controller.abort(), timeoutMs);

// 파일 업로드: 30초 타임아웃
const timeoutId = setTimeout(() => controller.abort(), 30000);
```

**의미**: API 요청에 10초, 파일 업로드에 30초 타임아웃이 설정되어 있다는 것은 네트워크 지연을 고려한 것입니다. S3 직접 접근 시 지연이 발생할 수 있음을 시사합니다.

#### 네트워크 에러 처리
```typescript
// src/api/client.ts:421
console.error('[API Client] Request failed:', {
  errorType: error instanceof TypeError ? 'NetworkError' : 'TimeoutError'
});

if (error instanceof TypeError && error.message.includes('Network')) {
  errorMessage = '네트워크 연결에 실패했습니다.\n인터넷 연결을 확인해주세요.';
}
```

**의미**: NetworkError, TimeoutError 등 네트워크 관련 에러 처리가 구현되어 있어, 실제로 네트워크 지연 문제가 발생할 수 있음을 보여줍니다.

### 아키텍처적 근거

#### S3 리전 종속성
- **현재 구조**: S3 버킷이 특정 리전(ap-northeast-2)에 위치
- **문제**: 
  - 한국 사용자: 상대적으로 빠름 (약 50-100ms)
  - 해외 사용자: 매우 느림 (500ms-2초 이상)
  - 모바일 네트워크: 추가 지연 발생 가능

#### 반복 요청 비효율
```typescript
// 같은 이미지를 여러 사용자가 요청해도 매번 S3까지 직접 접근
<Image source={{ uri: s3PhotoUrl }} />  // S3 직접 URL
```

**문제점**:
- 커뮤니티 게시글 썸네일: 여러 사용자가 동일 이미지 조회 → 매번 S3 접근
- 인기 미션 인증 사진: 반복 조회 → 불필요한 네트워크 홉
- 캐릭터 이미지: 모든 사용자가 동일 이미지 로드 → 캐싱 없음

## 2. 비용 비효율의 근거

### S3 데이터 전송 비용 구조

#### 현재 상황
- **S3 직접 접근**: 모든 요청이 S3로 직접 전달
- **비용 계산**:
  - S3 데이터 전송: $0.09/GB (첫 10TB, ap-northeast-2)
  - 사용자 1,000명이 하루 평균 10MB 이미지 조회 시
  - 일일: 1,000 × 10MB = 10GB
  - 월간: 10GB × 30일 = 300GB
  - 월간 비용: 300GB × $0.09 = **$27/월**

#### CloudFront 도입 후 예상
- **캐싱 효율**: 80-90% 요청이 엣지에서 처리 (보수적 추정)
- **실제 S3 요청**: 300GB × 10% = 30GB
- **CloudFront 비용**: 
  - 데이터 전송: $0.085/GB (첫 10TB)
  - 요청: $0.0075/10,000건
- **월간 비용**: 
  - S3: 30GB × $0.09 = $2.7
  - CloudFront: 300GB × $0.085 = $25.5
  - 총: **$28.2/월**

**비용 절감**: 초기에는 비슷하지만, 트래픽 증가 시 CloudFront 캐싱 효율이 높아져 비용 절감 효과가 커집니다.

### 반복 요청으로 인한 비용 증가

#### 시나리오 분석
```
인기 게시글 이미지 (1MB):
- 조회 1,000회/일
- S3 직접 접근: 1,000회 × 1MB = 1GB/일 = 30GB/월
- 비용: 30GB × $0.09 = $2.7/월

CloudFront 캐싱 (90% 캐시 히트):
- S3 요청: 100회 × 1MB = 0.1GB/월
- CloudFront: 1,000회 × 1MB = 1GB/월 (캐시)
- 비용: (0.1GB × $0.09) + (1GB × $0.085) = $0.094/월

절감: $2.7 - $0.094 = $2.6/월 (96% 절감)
```

## 3. 성능 문제의 근거

### 모바일 네트워크 환경

#### 현재 구조의 한계
- **원본 이미지 그대로 제공**: 최적화 없음
- **모바일 데이터 요금제**: 사용자 데이터 사용량 증가
- **불안정한 네트워크**: 지연 및 실패 가능성

#### 실제 사용자 경험
```typescript
// 이미지 로딩 시 사용자 경험
<Image source={{ uri: imageUrl }} />  
// → 네트워크 요청 → S3 응답 대기 → 렌더링
// 지연 시간: 200ms-2초 (네트워크 상황에 따라)
```

**문제점**:
- 스크롤 시 이미지가 순차적으로 로드되며 지연 발생
- 저사양 기기/느린 네트워크에서 더욱 두드러짐
- 사용자 이탈 가능성 증가

## 4. 확장성 문제

### 사용자 증가 시 예상 문제

#### 현재 구조
- 사용자 수 증가 → S3 요청 증가 → 비용 선형 증가
- 동일 이미지 반복 요청 → 불필요한 비용 발생
- 리전 제약 → 해외 사용자 경험 저하

#### CloudFront 도입 시
- 사용자 수 증가 → 캐싱 효율 증가 → 비용 증가율 완화
- 반복 요청 → 엣지 캐싱 → S3 요청 최소화
- 전 세계 엣지 → 일관된 성능 제공

## 결론

코드 분석과 아키텍처 분석을 통해 확인된 근거:

1. **지연 시간**: 타임아웃 설정, 네트워크 에러 처리로 실제 지연 발생 가능성 확인
2. **비용 비효율**: 반복 요청 시 캐싱 없이 S3 직접 접근으로 비용 증가
3. **성능 문제**: 모바일 네트워크 환경에서 최적화 부재
4. **확장성**: 사용자 증가 시 비용 및 성능 문제 악화 예상

CloudFront 도입은 이러한 문제들을 해결하고, 장기적으로 비용 절감과 성능 향상을 동시에 달성할 수 있는 솔루션입니다.
