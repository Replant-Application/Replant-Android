# CloudFront 도입 배경: 모바일 앱의 이미지 로딩 성능 개선을 위한 선택

## 현재 상황

Replant 앱은 모든 이미지 콘텐츠를 AWS S3에 저장하고, S3의 직접 URL을 통해 이미지를 로드하고 있습니다. 이미지 콘텐츠는 크게 두 가지로 구분됩니다:

1. **캐릭터 이미지(펫 이미지)**: 레벨별 캐릭터 애니메이션 및 정적 이미지 (현재는 앱 번들에 포함되어 있으나, CloudFront 도입과 함께 S3로 이전 예정)
2. **사용자 생성 이미지 콘텐츠**: 미션 인증 사진, 커뮤니티 게시글 이미지 등

초기 개발 단계에서는 S3의 단순한 스토리지 기능만으로도 충분했지만, 사용자 수가 증가하고 이미지 콘텐츠가 늘어나면서 몇 가지 성능 및 사용자 경험 문제가 드러나기 시작했습니다. 이번 CloudFront 도입을 통해 모든 이미지 콘텐츠의 성능을 개선하고, 동시에 앱 크기 감소 및 이미지 관리의 유연성을 확보하고자 합니다.

## 문제점

### 1. 지리적 거리로 인한 지연 시간 증가

S3 버킷이 특정 리전(예: ap-northeast-2)에 위치해 있을 경우, 해당 리전에서 멀리 떨어진 사용자들은 이미지 로딩 시 상대적으로 긴 지연 시간을 경험하게 됩니다. 특히 모바일 네트워크 환경에서는 이러한 지연이 더욱 두드러지며, 사용자가 스크롤할 때마다 이미지가 느리게 로드되는 현상이 발생합니다.

### 2. 반복 요청에 대한 비효율성

같은 이미지를 여러 사용자가 반복적으로 요청하더라도, S3로의 직접 요청은 매번 원본 서버까지의 네트워크 홉을 거쳐야 합니다. 커뮤니티 게시글의 썸네일이나 인기 있는 미션 인증 사진처럼 자주 조회되는 이미지의 경우, 이러한 반복 요청이 불필요한 네트워크 비용과 지연을 야기합니다.

### 3. 모바일 네트워크 대역폭 제약

모바일 앱 사용자들은 제한된 데이터 요금제를 사용하거나 불안정한 네트워크 환경에 노출되어 있습니다. S3에서 직접 이미지를 다운로드할 때는 이미지 최적화나 적응형 스트리밍 같은 기능이 없어, 사용자의 네트워크 상황에 맞춘 최적화된 콘텐츠 제공이 어렵습니다.

### 4. 트래픽 비용 증가

사용자 수가 증가할수록 S3로의 직접 요청이 늘어나며, 이는 데이터 전송 비용의 증가로 이어집니다. 특히 이미지와 같은 대용량 정적 콘텐츠의 경우, 트래픽 비용이 빠르게 증가할 수 있습니다.

## CloudFront 도입의 이점

### 1. 전 세계 엣지 로케이션을 통한 낮은 지연 시간

CloudFront는 전 세계에 분산된 엣지 로케이션(Edge Location)을 통해 사용자와 가장 가까운 위치에서 콘텐츠를 제공합니다. 이를 통해 이미지 로딩 시간을 크게 단축할 수 있으며, 특히 해외 사용자나 다양한 지역의 사용자를 대상으로 하는 서비스에서 그 효과가 두드러집니다.

### 2. 자동 캐싱으로 인한 성능 향상

CloudFront는 자동으로 콘텐츠를 엣지 서버에 캐싱하여, 동일한 이미지에 대한 반복 요청을 원본 서버(S3)까지 가지 않고 엣지에서 즉시 응답할 수 있습니다. 이는 응답 시간 단축뿐만 아니라 S3의 부하 감소와 데이터 전송 비용 절감에도 기여합니다.

### 3. 이미지 최적화 및 변환 기능

CloudFront는 Lambda@Edge나 CloudFront Functions를 활용하여 이미지 포맷 변환, 리사이징, 압축 등의 최적화 작업을 엣지에서 수행할 수 있습니다. 이를 통해 모바일 네트워크 환경에 최적화된 이미지를 제공하고, 사용자의 데이터 사용량을 줄일 수 있습니다.

### 4. HTTPS 및 보안 강화

CloudFront는 기본적으로 HTTPS를 지원하며, SSL/TLS 인증서를 무료로 제공합니다. 또한 DDoS 공격 방어, WAF(Web Application Firewall) 연동 등의 보안 기능을 통해 S3 버킷을 직접 노출하지 않고도 안전하게 콘텐츠를 제공할 수 있습니다.

### 5. 비용 최적화

CloudFront의 데이터 전송 비용이 S3의 데이터 전송 비용보다 저렴한 경우가 많으며, 캐싱을 통해 S3로의 요청 자체를 줄임으로써 전체적인 인프라 비용을 절감할 수 있습니다. 특히 트래픽이 많은 서비스에서는 이러한 비용 절감 효과가 누적되어 상당한 금액이 됩니다.

## 적용 범위

CloudFront는 S3에 저장된 모든 이미지 콘텐츠에 적용됩니다:

### 사용자 생성 이미지 콘텐츠
- 미션 인증 사진 (`mission_verify` 폴더)
- 커뮤니티 게시글 이미지 (`REPLANT/COMMUNITY` 폴더)
- 기타 사용자가 업로드한 이미지

### 캐릭터 이미지 (펫 이미지)
- 레벨별 캐릭터 애니메이션 GIF (`characters/level{1-5}/default.gif`, `happy.gif`)
- 캐릭터 정적 이미지 PNG (`characters/level{1-5}/default_static.png`)

### 캐릭터 이미지를 S3/CDN으로 옮기는 이유

초기에는 캐릭터 이미지를 앱 번들에 포함시켜 오프라인 지원과 즉시 로딩의 이점을 얻었습니다. 그러나 CloudFront 도입과 함께 캐릭터 이미지도 S3/CDN으로 옮기기로 결정한 이유는 다음과 같습니다:

1. **앱 크기 감소**: GIF 파일들이 앱 번들에서 제거되면 APK/IPA 크기가 크게 줄어들어 다운로드 시간과 저장 공간을 절약할 수 있습니다.

2. **이미지 업데이트 유연성**: 캐릭터 이미지에 버그가 있거나 개선이 필요할 때, 앱 업데이트 없이 S3의 이미지만 교체하면 모든 사용자에게 즉시 반영됩니다.

3. **동적 콘텐츠 제공**: 이벤트 스킨, 시즌별 캐릭터, 사용자 커스터마이징 등 다양한 캐릭터 변형을 서버에서 동적으로 제공할 수 있습니다.

4. **CloudFront 캐싱 효과**: 캐릭터 이미지는 모든 사용자가 반복적으로 로드하는 고정 콘텐츠이므로, CloudFront의 엣지 캐싱을 통해 매우 빠른 로딩 속도를 제공할 수 있습니다. 첫 로드 후에는 엣지에서 즉시 제공되므로 로컬 assets과 유사한 성능을 기대할 수 있습니다.

5. **일관된 아키텍처**: 모든 이미지가 동일한 방식(S3 + CloudFront)으로 관리되어 인프라 구조가 단순해지고 유지보수가 용이해집니다.

6. **비용 최적화**: 캐릭터 이미지는 고정적이고 자주 로드되므로 CloudFront 캐싱 효율이 매우 높아, 실제 S3로의 요청은 거의 발생하지 않아 비용이 최소화됩니다.

## 결론

S3를 통한 직접 이미지 제공은 초기 개발 단계에서는 충분한 솔루션이었지만, 서비스가 성장하고 사용자 경험에 대한 요구사항이 높아짐에 따라 더 나은 성능과 효율성을 제공할 수 있는 CDN의 도입이 필요해졌습니다. CloudFront를 도입함으로써 우리는 모든 이미지 콘텐츠(캐릭터 이미지 및 사용자 생성 이미지)에 대해 더 빠르고 안정적인 로딩 경험을 제공할 수 있으며, 동시에 앱 크기 감소, 이미지 업데이트 유연성 확보, 인프라 비용 최적화, 그리고 확장 가능한 아키텍처 구축을 달성할 수 있게 되었습니다.
